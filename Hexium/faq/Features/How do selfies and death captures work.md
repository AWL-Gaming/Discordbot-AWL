The `selfie` in-game command or configured selfie hotkey captures the current game view and posts it to the configured chat webhook.

The death feed can attach a screenshot or short GIF when those options are enabled. Capture resolution, GIF frame rate, duration, and screenshot delay are client-local settings. Larger attachments are transferred to the server before the server posts them to Discord.
