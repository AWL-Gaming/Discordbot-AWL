The main config file is:

`BepInEx/config/RustyMods.DiscordBot.cfg`

Webhook URLs and the Discord bot token belong in the dedicated server config. Client capture settings such as screenshot and GIF options remain local to each client.
